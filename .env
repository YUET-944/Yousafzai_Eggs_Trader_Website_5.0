# Leave VITE_API_BASE empty so the frontend uses same-origin requests
# proxied through vercel.json rewrites in production and vite proxy in dev.
# Set this only when you need to call the backend directly:
#   VITE_API_BASE=https://shayan-ali832-yafbackend.vercel.app
VITE_API_BASE=
